# Configuração de deploy da Netlify
# Site estático (HTML/CSS/JS) — não há etapa de build.
# O conteúdo do site fica na subpasta "docs/" (compartilhada com o GitHub Pages).
[build]
  publish = "docs"
